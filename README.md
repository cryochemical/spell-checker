# spell-checker
Allows the user to add custom words to a Spelling Tree - a tree where each node contains a letter a-z. Then, the user can efficiently check whether or not a word is contained in the tree in logn time.
